# MiniMax API key for /api/mood-search (server-side only, never expose to client)
MINIMAX_API_KEY=your-key-here
# Optional overrides
# MINIMAX_BASE_URL=https://api.minimax.io
# MINIMAX_MODEL=MiniMax-M2
